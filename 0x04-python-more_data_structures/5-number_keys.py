#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    number_keys - get a directory keys length
    @a_directory: the directory
    Return: the directory keys length
    """
    return len(a_dictionary.keys())
